import { SwipeEngine } from "@/components/SwipeEngine";

export default function Home() {
  return (
    <main className="mx-auto flex w-full max-w-lg min-h-screen flex-1 justify-center">
      <SwipeEngine />
    </main>
  );
}
